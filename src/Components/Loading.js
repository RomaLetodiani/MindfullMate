import React from 'react';

const Loading = () => {
  return (
    <div className="text-4xl text-[#fff] flex justify-center items-center">
      <p className="border-dashed border-2 p-10 rounded-full">loading...</p>
    </div>
  );
};

export default Loading;
